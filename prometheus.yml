global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'job-board-worker'
    static_configs:
      - targets: ['api:8000'] # Assuming FastAPI exports worker metrics too
